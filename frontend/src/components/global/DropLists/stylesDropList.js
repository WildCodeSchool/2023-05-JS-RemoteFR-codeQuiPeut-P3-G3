/* styles pour droplists */
export const stylesDLAdminActions = {
  valueContainer: (base) => ({
    ...base,
    maxHeight: 20,
    fontSize: 15,
  }),
  menuList: (base) => ({
    ...base,
    maxHeight: 120,
    //   maxWidth: 130,
  }),
  control: (base) => ({
    ...base,
    maxHeight: 15,
    maxWidth: 100,
    minWidth: 80,
    alignItems: "center",
  }),
}
